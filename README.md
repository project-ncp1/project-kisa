본 스크립트는 Linux(Bash) 환경에서 동작하도록 작성되었습니다.
Rocky Linux 환경에서 SSH 접속 후 테스트를 완료하였습니다.

또한 개발 환경이 macOS인 점을 고려하여,
OSTYPE 기반 분기 처리를 통해 macOS(darwin)와 Linux 환경을 구분하도록 구현하였습니다.

## 실행 방법
chmod + x project-kisa.sh
./project-kisa.sh
