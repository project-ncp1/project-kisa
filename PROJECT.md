KISA UNIX 취약점 점검 자동화 스크립트 (U-07, U-08, U-10, U-15, U-20)
<프로젝트 개요>
본 프로젝트는
KISA 주요 UNIX 취약점 항목을 기준으로,
운영체제 환경(macOS / Linux)을 고려한
계정·권한·파일·서비스 보안 상태를 자동 점검하기 위해 제작한
Bash 기반 보안 진단 스크립트입니다.
--------------------------------------------------------
<1.OS 환경 구분 및 그룹 허용 정책 분기>
-점검 목적-

운영체제(macOS / Linux)에 따라
시스템 기본 그룹 정책이 다르다는 점을 반영하여
잘못된 권한 판정을 방지하기 위함

- 구현 이유 -

macOS는 wheel 그룹이 관리자 역할을 수행

Linux는 기본적으로 root 그룹만 관리자 그룹으로 사용

실습 환경이 macOS 노트북이었기 때문에
리눅스 기준 스크립트를 그대로 사용할 수 없었음

- 보안적 의미 -

OS 환경을 고려하지 않으면 정상 시스템을 오탐할 수 있음

2.root 계정 로그인 상태 점검 (U-07)
--------------------------------------------------------
<점검 목적>
Root 계정 직접 로그인을 차단하여
계정 탈취 및 브루트포스 공격 위험을 최소화

<점검 기준 및 구현 설명>
- /etc/shadow 파일에서 root 계정의 비밀번호 필드를 확인,
비밀번호가 !!로 설정되어 있는지 여부를 점검하여 !!로 설정된 경우 비밀번호 기반 인증이 잠금 상태로
root 계정의 직접 로그인이 차단된 것으로 판단.  

<구현 이유>
- root 계정은 최고 권한 계정이므로 공격 성공 시 피해가 크기 때문.

- password 기반 로그인 방식은 브루트포스 공격에 취약하기 때문.

<보안적 의미>
root 계정의 직접 로그인을 차단함으로써,
외부 공격자가 시도할 수 있는 침입 경로를 줄이고,
시스템의 공격 표면을 효과적으로 축소할 수 있다.
 
<3./etc/shadow 파일 권한 점검 (U-08)>
---------------------------------------------------------
- 점검 목적 -
시스템 사용자 비밀번호 해시 정보가 저장된
/etc/shadow 파일의 기밀성과 무결성 보호

- 구현 이유 -
 /etc/shadow 파일의 소유자 및 권한을 확인하여
인증 정보가 저장된 파일에 대한 비인가 접근 및 변조를 방지하기 위한것으로
패스워드 크래킹 및 계정 탈취, 권한 상승 공격의 발판을 차단하기 위한 것입니다.

- 보안적 의미 -

권한 400 이하 + 소유자 root 유지가 원칙

계정 보안의 최후 방어선 역할
------------------------------------------------------------
4/etc/hosts 파일 소유자 및 권한 점검
- 점검 목적 -

시스템 네트워크 해석에 영향을 주는
/etc/hosts 파일 변조 여부 방지

- 구현 이유 -

/etc/hosts는 도메인 → IP 매핑에 직접 영향

변조 시:

트래픽 우회

피싱

악성 서버로의 리다이렉션 가능

- 보안적 의미 -

root 소유 / 600 이하 권한 유지

네트워크 기반 공격 사전 차단
------------------------------------------------------------------
<5.파일 그룹 소유자 점검 (U-10 연계)>
- 점검 목적 -

파일 실행 시 비정상적인 권한 상승 가능성 차단

- 구현 이유 -

파일 소유자는 root지만
그룹이 일반 사용자일 경우:

SGID, 실행 흐름에 따라

일반 사용자가 관리자 권한을 간접적으로 행사 가능

이는:

계정 탈취

시스템 설정 변경

네트워크 설정 변조로 이어질 수 있음

- 보안적 의미 -

소유자와 그룹 일관성은 권한 상승 공격 방지의 핵심

SUID/SGID 취약점 사전 탐지 목적
-------------------------------------------------------------------
<6.World Writable 파일 점검 (U-15)>
 - 점검 목적 -

모든 사용자가 수정 가능한 파일로 인한
무결성 훼손 및 실행 경로 공격 위험 식별

- 구현 이유 -

World Writable 자체가 즉시 권한 상승을 의미하지는 않음

그러나 공격자는 이를 이용해:

설정 파일 변조

실행 파일 악성 코드 삽입

로그 삭제 및 조작

서비스 장애 유발 가능

특히 root 또는 cron이 실행하는 경로에 포함될 경우 고위험

- 보안적 의미 -

World Writable은 “조건”이지 “결과”가 아님

실행 경로 포함 여부를 기준으로 위험도 판단 필요

기본 시스템 디렉터리는 예외 처리하여 오탐 방지
-----------------------------------------------------------------------
<7.Anonymous FTP 계정 및 서비스 점검 (U-20)>
- 점검 목적 - 

익명 사용자 접근을 통한
데이터 유출 및 무결성 훼손 방지

- 구현 이유 -

Anonymous FTP가 활성화되면:

인증 없이 파일 접근 가능

공격자가 데이터 업로드/다운로드 가능

이는:

기밀성

무결성

가용성 모두에 위협

- 보안적 의미 -

FTP 계정 존재 여부

FTP 설정 파일 내 anonymous_enable 설정

FTP 서비스 실행 여부를 종합적으로 판단

“미설치 = 양호” 개념 반영
------------------------------------------------------------------------------
프로젝트 설계 요약

본 프로젝트는
단순히 취약점 존재 여부만 판단하지 않고,
운영체제 환경, 실행 경로, 권한 흐름을 함께 고려하여
실제 공격 가능성이 있는 항목을 식별하는 데 목적을 두었습니다.
